# cs2-autoexec-generator
A browser-based tool to **import, merge, edit, and export** Counter-Strike 2 configuration files (`.vcfg`, `.cfg`).   Generate a clean, optimized `autoexec.cfg` directly in your browser — no installation required.
